/**
 * The `Menu` package contains classes that define user interfaces and menus for interacting with the system.
 * This package serves as the user-facing interface to access various functionalities and features of the system.
 * It includes subpackages for specific menu categories, such as `CampHandler` for camp-related operations and `CommentHandler` for comment-related operations.
 * Users, including students, staff, and administrators, can use the classes in this package to navigate through the system, perform actions, and access information.
 * The classes here provide a structured and user-friendly way to interact with the underlying functionality of the system.
 */
package Menu;
