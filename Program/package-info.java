/**
 * The `Program` package includes the main entry point of the Camp Management System.
 * It contains the `MainProgram` class, which initializes and starts the application.
 * 
 * <p>The {@link MainProgram} class contains the `main` method, which is the starting point
 * of the entire application. It initializes the necessary components and user interfaces,
 * allowing users to interact with the Camp Management System.</p>
 * 
 * <p>This package serves as the entry point for the application and orchestrates the
 * initialization and execution of the system.</p>
 */
package Program;
